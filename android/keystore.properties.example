# 复制本文件为 keystore.properties（与本文件同目录：android/），填本地路径与别名。
# 不要把真实 keystore.properties 或 *.jks / *.keystore 提交进 Git。

storeFile=../keystore/account-release.jks
storePassword=REPLACE_STORE_PASSWORD
keyAlias=account
keyPassword=REPLACE_KEY_PASSWORD
